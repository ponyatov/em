line-length    = 80
indent-width   = 2
target-version = "py310"

ignore = ["E701"]

select = ["E", "F"]

exclude = [
    ".git",
    "ref",
    "__pycache__",
    "*.pyi",       # Stub files
    "migrations",  # Django migrations
]

[format]
quote-style  = "double"
indent-style = "space"
