#include <stdint.h>

%%{
    machine to_oct;

    oct_digits := |*
        [0-7] => { result = (result << 3) | (fc - '0'); };
    *|;

    main := |*
        '0' => { /* expect x next */ };
        'o' => { fgoto oct_digits; };
    *|;

}%%

%%write data;

uint32_t to_oct(char *ts, char *te) {
    char *p = ts, *pe = te, *eof = pe;
    int cs = to_oct_start; int act;
    int result = 0;
    
    %%write init;
    %%write exec;
    
    return result;
}
